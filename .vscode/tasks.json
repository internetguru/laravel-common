{
	"version": "2.0.0",
    "tasks": [
        {
            "label": "Build and Run Tests",
            "type": "shell",
            "command": "docker build -t laravel-common-test . && docker run --rm laravel-common-test",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": []
        }
    ]
}
